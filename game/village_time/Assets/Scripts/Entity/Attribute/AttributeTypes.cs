public static class AttributeTypes {
    public static class Attributes {
        public const Type MAX_HEALTH = typeof(MaxHealthCmpt);
    }

    public static class Resources {
        public const Type HEALTH = typeof(HealthCmpt);
    }
}